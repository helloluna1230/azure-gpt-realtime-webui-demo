{
  "name": "azure-gpt-realtime-webrtc-demo",
  "version": "0.1.0",
  "private": true,
  "description": "WebRTC demo for Azure GPT-realtime-2, GPT-realtime-translate, and GPT-realtime-whisper.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js && node --check scripts/test-translate-minimal.mjs",
    "test:translate:minimal": "node scripts/test-translate-minimal.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@azure/identity": "^4.13.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  }
}
